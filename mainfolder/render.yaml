services:
  - type: web
    name: fuel-receipt-generator
    env: node
    buildCommand: npm install
    startCommand: node mainfolder/server.js
    envVars:
      - key: NODE_ENV
        value: production
